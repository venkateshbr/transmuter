import { Component } from '@angular/core';

@Component({
  selector: 'app-people',
  standalone: true,
  template: `
    <div class="p-8">
      <h1 class="text-2xl font-semibold">People.</h1>
      <p class="text-[var(--t-text-secondary)] mt-2">Implementation coming in Phase 1.</p>
    </div>
  `,
})
export class PeopleComponent {}
